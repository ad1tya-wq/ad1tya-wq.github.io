# AFK log, 2026-09-14

Running record for Aditya to review later. Newest entries at the bottom. Each entry: what, why, evidence, and anything that needs a decision.

## Brief (from the user, verbatim intent)
- Separate branch: (1) portrait hover morphs as a wave from the pointer; (2) the black-hole "mass" game (spec in the plan file: preserve across scroll, reversible slider, topbar eaten last, Reset at max).
- Main: fix the Horizon slingshot (direction cannot be set); feasibility note (no implementation) for a realistic power/speed mechanism.
- Test rigorously; log every important change, roadblock, alternative and open decision here.

## Assumptions taken (no reply within the 4-minute window)
- The wave hover shipped on main in `d61b106` earlier today; if it is broken it is fixed on main and carried into the branch (a branch-only copy would leave main with the bug).
- Branch name `feature/devour`, pushed, not merged.
- Horizon aim becomes pointer-draggable with a visible aim line; keyboard aim stays.
- This file lives on main; the branch rebases/merges it in when it is created.

## 1. Wave hover: verified and slowed (main)
- Built a puppeteer-core harness (scratchpad, not in repo) because the in-app browser pane was hidden and GSAP does not tick in hidden tabs.
- Finding: the wave was implemented and running (state log 119 → 668 px over 0.55 s) but with `power2.out` over 0.6 s the first 100 ms already covered ~300 px, i.e. the whole face: it read as a cut. That matches the complaint.
- Change: constant-speed front, 640 px/s (`SPEED` in `src/islands/hero.ts`), `sine.out` in / `sine.in` out; a 740 px portrait takes ~1.1 s to fill. Frames at 256 ms and 562 ms show the circle growing from the pointer with the particle bust outside it.
- Decision for you: if 640 px/s feels slow, 900 px/s is the next notch I would try. One-line change.

## 2. Horizon slingshot: direction fix (main)
- Root cause of "cannot set a direction": dragging did work in code, but (a) a drag of 120 px produced a barely moving probe (velocity scale 0.03 √gm per r_s of drag), (b) there was zero feedback while aiming, (c) the drag selected the heading text, and (d) the keyboard aim was invisible unless you knew the arrow keys. It felt like nothing happened.
- Now: press anywhere in the section and drag; the predicted path (real integration of the same Paczyński-Wiita physics, `predict()` in `slingshot.ts`, unit-tested against `stepProbe`) is drawn as a dotted line while you drag, the status line reads `Aim 23°, 2.06 × escape speed`, release launches. 100 css px of drag ≈ 0.55 √gm; a tap under 12 px does nothing. Hovering or focusing the Launch button previews the default shot; arrow keys retune it with the same preview. Pointer capture keeps the drag alive outside the section; `preventDefault` stops text selection; touch cancel aborts cleanly. Probe points are 2.6 px.
- Also fixed on the way: landing on a deep link (`/#horizon`, or a reload mid-page) started the field at progress 0 (the star, not the hole) because `mountScroll` reset the target after creating the triggers. The initial target now comes from the active chapter.
- Evidence: harness run — during drag `Aim 23°, 2.06 × escape speed`; release → `Probe away.`; 4 s later → `Captured…`; screenshots `toy-dragging.png`, `toy-flight.png` (scratchpad).

### Feasibility: a realistic "power" mechanism (not implemented, as asked)
- The drag already is a power mechanism: drag length sets |v| linearly, drag direction sets the heading, and the readout expresses it as a multiple of the local escape speed √(2GM/(r−r_s)), so 1.0× is the physically meaningful threshold (below: bound, will fall in or orbit; above: leaves, unless it passes inside ~2 r_s where nothing helps).
- If you want an explicit control, three options in rising realism:
  1. **Charge bar (hold to charge).** Hold the launch button, a bar fills from 0 to 2× escape speed, release fires along the current aim. Cheap (≈40 lines), familiar, works on touch and keyboard (hold Space). Recommended if you want a "power" feel.
  2. **Δv budget.** Give the probe a fixed propellant budget (say 1.4× escape) and let the visitor split it between launch speed and a single mid-course burn triggered by a tap. Teaches the Oberth effect for free (burning near periapsis buys more). Medium (≈120 lines, needs a burn UI and a second preview).
  3. **Thrust profile.** Continuous thrust while a key is held, with mass depletion (rocket equation) shown in the readout. Most realistic, but it turns the toy into a game and the status line into a HUD; I would not do this on a portfolio.
- None of them needs shader work; all run through the existing `predict()`/`setProbe` path. My pick: option 1 if you want more control, otherwise leave the drag as is; it is already the physical model.
