# AFK log, 2026-09-14

Running record for Aditya to review later. Newest entries at the bottom. Each entry: what, why, evidence, and anything that needs a decision.

## Brief (from the user, verbatim intent)
- Separate branch: (1) portrait hover morphs as a wave from the pointer; (2) the black-hole "mass" game (spec in the plan file: preserve across scroll, reversible slider, topbar eaten last, Reset at max).
- Main: fix the Horizon slingshot (direction cannot be set); feasibility note (no implementation) for a realistic power/speed mechanism.
- Test rigorously; log every important change, roadblock, alternative and open decision here.

## Assumptions taken (no reply within the 4-minute window)
- The wave hover shipped on main in `d61b106` earlier today; if it is broken it is fixed on main and carried into the branch (a branch-only copy would leave main with the bug).
- Branch name `feature/devour`, pushed, not merged.
- Horizon aim becomes pointer-draggable with a visible aim line; keyboard aim stays.
- This file lives on main; the branch rebases/merges it in when it is created.

## 1. Wave hover: verified and slowed (main)
- Built a puppeteer-core harness (scratchpad, not in repo) because the in-app browser pane was hidden and GSAP does not tick in hidden tabs.
- Finding: the wave was implemented and running (state log 119 → 668 px over 0.55 s) but with `power2.out` over 0.6 s the first 100 ms already covered ~300 px, i.e. the whole face: it read as a cut. That matches the complaint.
- Change: constant-speed front, 640 px/s (`SPEED` in `src/islands/hero.ts`), `sine.out` in / `sine.in` out; a 740 px portrait takes ~1.1 s to fill. Frames at 256 ms and 562 ms show the circle growing from the pointer with the particle bust outside it.
- Decision for you: if 640 px/s feels slow, 900 px/s is the next notch I would try. One-line change.

## 2. Horizon slingshot: direction fix (main)
- Root cause of "cannot set a direction": dragging did work in code, but (a) a drag of 120 px produced a barely moving probe (velocity scale 0.03 √gm per r_s of drag), (b) there was zero feedback while aiming, (c) the drag selected the heading text, and (d) the keyboard aim was invisible unless you knew the arrow keys. It felt like nothing happened.
- Now: press anywhere in the section and drag; the predicted path (real integration of the same Paczyński-Wiita physics, `predict()` in `slingshot.ts`, unit-tested against `stepProbe`) is drawn as a dotted line while you drag, the status line reads `Aim 23°, 2.06 × escape speed`, release launches. 100 css px of drag ≈ 0.55 √gm; a tap under 12 px does nothing. Hovering or focusing the Launch button previews the default shot; arrow keys retune it with the same preview. Pointer capture keeps the drag alive outside the section; `preventDefault` stops text selection; touch cancel aborts cleanly. Probe points are 2.6 px.
- Also fixed on the way: landing on a deep link (`/#horizon`, or a reload mid-page) started the field at progress 0 (the star, not the hole) because `mountScroll` reset the target after creating the triggers. The initial target now comes from the active chapter.
- Evidence: harness run — during drag `Aim 23°, 2.06 × escape speed`; release → `Probe away.`; 4 s later → `Captured…`; screenshots `toy-dragging.png`, `toy-flight.png` (scratchpad).

### Feasibility: a realistic "power" mechanism (not implemented, as asked)
- The drag already is a power mechanism: drag length sets |v| linearly, drag direction sets the heading, and the readout expresses it as a multiple of the local escape speed √(2GM/(r−r_s)), so 1.0× is the physically meaningful threshold (below: bound, will fall in or orbit; above: leaves, unless it passes inside ~2 r_s where nothing helps).
- If you want an explicit control, three options in rising realism:
  1. **Charge bar (hold to charge).** Hold the launch button, a bar fills from 0 to 2× escape speed, release fires along the current aim. Cheap (≈40 lines), familiar, works on touch and keyboard (hold Space). Recommended if you want a "power" feel.
  2. **Δv budget.** Give the probe a fixed propellant budget (say 1.4× escape) and let the visitor split it between launch speed and a single mid-course burn triggered by a tap. Teaches the Oberth effect for free (burning near periapsis buys more). Medium (≈120 lines, needs a burn UI and a second preview).
  3. **Thrust profile.** Continuous thrust while a key is held, with mass depletion (rocket equation) shown in the readout. Most realistic, but it turns the toy into a game and the status line into a HUD; I would not do this on a portfolio.
- None of them needs shader work; all run through the existing `predict()`/`setProbe` path. My pick: option 1 if you want more control, otherwise leave the drag as is; it is already the physical model.

## 3. Branch `feature/devour`: the mass slider game (first working build, `61ff923`)
Branched from main after the wave and slingshot fixes, so it contains both.

What is in it:
- Horizon now has two controls under the hole: Launch (left) and **Mass** (right; a native range slider, hairline track, core thumb, readout `1.0 → 10.0 M☉`, `aria-valuetext` in solar masses).
- Mass scales the hole 1× → 2.5× (shadow, photon ring, lensing, disk radius) and the slingshot gravity (`massScale()` in `lifecycle.ts`; the toy's `gm` uses it, so probes get captured more easily at high mass).
- Reach: `reach(mass)` grows as `((m − 0.04)/0.96)^1.5 × farthest`, so nothing happens in the first 4 % and everything is within reach at 1. Blocks (every leaf text element in `main`, the portrait, then the contact details, then the topbar) are ordered by document distance from the hole and eaten in that order; hysteresis of 24 px stops flicker at a threshold. 112 blocks on desktop, 114 on a phone (the readout is hidden there; the inline project details are not).
- Eating a block: its glyphs are sampled with the hero-name sampler, the DOM copy goes `visibility: hidden` in place (no reflow, no scroll jump), and the particles fly a decaying orbit into the disk over 1.6 s (radius eases in, the spin rate rises as it gets closer, a golden-ratio lag smears each word into a streak, then the particles hand over to the Keplerian disk and stay there). Frames in the harness show spiral arms of text wrapping into the disk (`dv-250-1000.png`).
- Preserve across scroll: yes. Scrolling back up after feeding shows sections with only their hairlines left.
- Reversible: lowering the slider restores blocks in reverse; particles fly the orbit backwards out of the disk to the glyphs, then the DOM copy reappears. A block interrupted mid-flight reverses from where it is (no jump).
- Void: at mass 1 with everything gone the status reads "Nothing escapes." and `Reset the universe?` appears after the last flight; it sets `history.scrollRestoration = 'manual'` and reloads, landing at the top with the name re-assembling. Verified: after reset scrollY 0, nothing eaten, slider at 0.
- The hero name and portrait, when eaten, also hide their resting particles (`uEatenNameFace`), so the hero does not silently rebuild itself on scroll-up.
- Reduced motion: hole scales, blocks fade (400 ms) instead of flying, reset appears after the fade. Verified 83 blocks eaten at 0.7 under `prefers-reduced-motion`.
- Particle pool: the last 20 % of the budget (≈10k on desktop, 4k on phones), ring-allocated; a block gets area/8 points (60–3000). When the ring wraps the oldest block loses its slots (its particles are already in the disk, indistinguishable); if such a block is later restored it fades back in instead of flying. Fine for the effect, noted here as a known simplification.
- Budget: 63.5 KB gz (+2.7 KB). 44 unit tests (ordering, reach, hysteresis, points, ring pool, predict, escape speed). Impeccable detector: 0 findings.

Bugs found and fixed during testing:
- Eaten text vanished with no particles: a freshly allocated block was measured as a flight that started at shader time 0, so it was already "in the disk". Fixed by measuring before allocating.
- The first density (area/32, pool 10 %) was invisible against the field: one or two points per glyph. Now area/8 and a 20 % pool.
- `hidden` on the reset button was overridden by the `.btn` display rule: the button showed at every mass. Fixed.
- The mass control made the Horizon column taller and pushed the launch button into the hole; controls are now a two-column row under the caption.
- The launch button was not being eaten (`a.btn` vs `button.btn`).

Decisions you may want to revisit (all one-line changes):
- Contact details are eaten in their own tier after the rest of the page (recruiter safety), topbar last. If you would rather have pure distance order, drop the tier in `devour/index.ts`.
- Eaten particles sit in the disk at every chapter, so after feeding, even the hero shows a ring of eaten text around the star. I like it (the object carries what it took); if you do not, gate `hasEat` by `wHole` in the shader.
- Flight duration 1.6 s, 2–5 turns. Fewer turns = less dramatic, more "falls straight in".
- The dev-toolbar row of icons at the bottom of every dev screenshot is Astro's dev toolbar, not the site.

Not done / open:
- The Astro dev server keeps serving stale scoped CSS after component rewrites (restart needed each time); known, not a site issue.
- Lighthouse on the branch not re-run yet (next).

## 4. Branch follow-ups
- Slider is now 0–100 in integer percent (keyboard: 100 arrow presses across the range, PageUp/PageDown 10 %). The earlier 0–1000 scale needed 60 presses to reach 6 %.
- Certificate seals fade with their plates; faded blocks ignore the pointer; the toy's aim gesture ignores the mass control (a drag on the MASS label no longer launches a probe: verified).
- Block ordering is measured once on first use; a later resize does not reshuffle already-eaten blocks.
- Cost of jumping straight to full mass (End key): 125 ms to sample all 112 blocks in one go; dragging spreads it over the gesture. Restore is 0.7 ms (no sampling). Acceptable; if it ever matters, sampling can be spread across frames.
- Interaction matrix run (no console errors): keyboard End/Home/arrows; reversal mid-flight (600 → 200 → 600); nav link while partly eaten; project pictograms still activate; touch viewport 390×844: eat, void, reset (lands at top, nothing eaten, slider 0); reduced motion path.
- Lighthouse mobile on the branch build: 72 perf / 100 a11y (main 77; the two runs are within the harness's noise, and the slider adds no work until touched).
- Branch pushed: https://github.com/ad1tya-wq/ad1tya-wq.github.io/tree/feature/devour (open a PR from there when you want to review; I did not merge).

## Where things stand
- main: wave fix, slingshot fix, deep-link fix, this log.
- feature/devour: all of main plus the mass slider game and its spec addendum.
- Still parked: soft skills (your call at the very end); the realistic power mechanism (feasibility above, not built, as asked).

## 5. Branch: the pool was eating the star
- Found while checking the hero after a full feed: the reserved pool was the last 20 % of the buffer, which is exactly the resting star (name 42 % + portrait 38 % + star 20 %). Feeding the hole was quietly dismantling the star at the top of the page.
- Fix: budget raised 20 % (`clamp(area × 0.06, 24k, 96k)`) with an explicit split in one place (`SLOTS` in `layout.ts`: name 35 %, portrait 32 %, eaten text 15 %, star 18 %). Absolute name/portrait counts are unchanged; the star keeps its 18 % at all times. Verified: with name and portrait hidden the star is a clean 256 px ball at the hero.
- GPU cost: +20 % points in one draw call; no measurable frame-time change in the harness.

## 6. Branch: self-review fixes (a reviewer agent was cut off by the usage limit, so I did the pass by hand)
- Restoring a block waited 1.6 s, but the most lagged particle leaves 0.35 s late: the text reappeared while its last particles were still flying back. Restore and the reset button now wait 1.95 s.
- The `aria-live` hint was rewritten on every slider tick even when unchanged; it now updates only when the text changes (screen readers were going to hear "44 of 116 blocks taken" on every pixel of drag).
- Some browsers restore form values across a reload, so after Reset the slider could show 100 % while the page was whole; the slider is forced to 0 on mount and has `autocomplete="off"`. `history.scrollRestoration` is set back to `auto` on mount so ordinary back/forward navigation keeps working after a Reset.
- Checked and fine: `bufferSubData` byte offsets (16 B per particle), ring-pool eviction vs. restore (evicted blocks fade back instead of flying), hysteresis, GLSL `smoothstep` edge order, mount without WebGL (falls back to fades; Reset still works).
- Interaction matrix re-run after the fixes: clean, no console errors.

## 7. Devour v2 (branch): readable bodies, dust only at the end, the void
- Your note: everything turned to dust at once, and with the slider at the bottom nothing was noticeably "sucked away". Feasibility: a rigid-body particle version (all of a block's particles sharing one translate + shrink) would need ~1 particle per 3 px² to stay readable, i.e. tens of thousands of particles for a paragraph, beyond the pool. So the readable act is now the real DOM: the block itself flies toward the hole (GSAP transform, 1.8 s, accelerating), shrinking to 30 % and thinning to 55 % opacity; blocks far above enter from the top edge and are legible all the way. It arrives just outside the shadow on the side it came from (5.5 r_s, scaled with mass; blocks already that close only shrink in place) and only then becomes particles: a 1.2 s spiral of bright dust (brighter and larger than the disk it falls through) that settles into the disk. Reversal runs both acts backwards (dust out, body home), including mid-flight.
- Full mass: after the last block's dust settles (~3.5 s) the screen goes pure black (1.4 s fade), the slider is disabled (irreversible), and only "Reset the universe?" remains, floating gently at the centre with focus on it. Reset reloads to the top.
- Harness evidence: `legible-900.png` shows "Horizon", the caption and the launch button in flight and the Class X block entering from the top; `dust-2100.png` shows the dust comet spiralling in; `void.png` the black screen with the button. Interaction matrix: no errors, restore to 0 clean.
- Also done: Co-Authored-By trailer removed from history on both branches (amended + force-pushed, reflog expired locally; GitHub may keep the orphaned object briefly but nothing references it); Soft skills band trimmed to Communication, Organisation, Cross-team collaboration, Working to deadlines, Mentoring peers.
